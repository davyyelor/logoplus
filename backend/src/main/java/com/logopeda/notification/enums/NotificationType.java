package com.logopeda.notification.enums;

/** Category of an in-app notification. */
public enum NotificationType {
    APPOINTMENT_REMINDER,
    APPOINTMENT_CANCELLED,
    REPORT_SHARED,
    DOCUMENT_SHARED,
    CONSENT_REQUESTED,
    GENERAL
}
